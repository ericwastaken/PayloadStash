# Template for secrets.env (which is git-ignored). The run scripts copy this to
# secrets.env on first use. Contains only the local docker broker default — replace
# RMQ_URI with your own broker (and never commit real credentials).
RMQ_URI=amqp://guest:guest@localhost:5672/%2F
