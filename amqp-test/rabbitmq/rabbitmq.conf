# Allow the default guest user to connect from outside the container (host -> mapped port).
# By default guest is restricted to loopback; a host connection to the mapped port is NOT
# loopback from the broker's perspective, so this is required for local testing.
loopback_users = none
