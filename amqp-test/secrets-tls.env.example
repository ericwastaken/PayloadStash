# Template for secrets-tls.env (which is git-ignored). The TLS run script copies this to
# secrets-tls.env on first use. Contains only the local docker broker default — replace
# RMQ_URI with your own broker (and never commit real credentials).
RMQ_URI=amqps://guest:guest@localhost:5671/%2F
